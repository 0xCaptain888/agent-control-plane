# Dashboard

# Dashboard

Implemented static reference dashboard for action state, risk decisions, execution
results, receipts, and recovery events.

Open `apps/dashboard/src/index.html` directly in a browser. The fixture data mirrors
the OKX, Agent Commerce, and Treasury Guard demos and can later be replaced with a
receipt API without changing the visual contract.
