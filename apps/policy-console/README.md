# Policy Console

# Policy Console

Implemented static reference UI for authoring and versioning Agent policies without
changing application code.

Open `apps/policy-console/src/index.html` directly in a browser to validate a JSON
policy before publishing it to the control plane.
