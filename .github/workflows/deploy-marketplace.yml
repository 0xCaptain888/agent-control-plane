name: Deploy Marketplace

on:
  push:
    branches: [main]
    paths:
      - "apps/marketplace/src/**"
      - "docs/**"
      - "deployments/*.json"
      - "evidence/judge/*.json"
      - "assets/social/**"
      - ".github/workflows/deploy-marketplace.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Build judge site artifact
        run: |
          mkdir -p _site/docs _site/deployments _site/evidence/judge _site/assets/social
          cp -R apps/marketplace/src/. _site/
          cp -R docs/. _site/docs/
          cp deployments/*.json _site/deployments/
          cp evidence/judge/*.json _site/evidence/judge/
          cp assets/social/*.svg _site/assets/social/
      - name: Upload Marketplace
        uses: actions/upload-pages-artifact@v5
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
