# Solana adapter

# Solana adapter

Implemented reference adapter for Solana programs, vaults, and transaction
simulation. Inject a `SolanaClient` so the control plane stays independent of
Anchor, web3.js, and wallet credentials.
