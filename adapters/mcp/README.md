# MCP adapter

# MCP adapter

Implemented reference adapter for exposing already-approved control-plane actions
as MCP tool calls. The adapter never decides whether a tool is allowed; that is the
policy engine's responsibility.
