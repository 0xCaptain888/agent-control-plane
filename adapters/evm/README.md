# EVM adapter

# EVM adapter

Implemented reference adapter for contract calls, swaps, transfers, and treasury
actions on EVM-compatible networks. Inject an `EvmClient` for viem, ethers, or a
wallet/simulation service; policy and risk decisions remain in `packages/`.
