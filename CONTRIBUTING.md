# Contributing

Keep core packages domain-neutral. New exchange, chain, payment, or tool integrations belong under `adapters/`. New hackathon narratives belong under `examples/` and must reuse the same Intent → Policy → Risk → Execute → Verify → Recover → Receipt lifecycle.
