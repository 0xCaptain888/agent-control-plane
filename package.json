{
  "name": "@captain/agent-control-plane",
  "private": true,
  "version": "0.1.0",
  "description": "Policy-controlled, verifiable execution infrastructure for autonomous AI agents.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test examples/safe-trade/test/*.test.mjs examples/api-procurement/test/*.test.mjs",
    "lint": "echo 'Add repository lint configuration'"
  },
  "workspaces": [
    "packages/*",
    "adapters/*",
    "examples/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
