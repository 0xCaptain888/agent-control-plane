# Observability boundary

Dependency-free structured logs and Prometheus-compatible metrics. Deployments
can forward the output to OpenTelemetry, Prometheus, Grafana, or a managed
provider without coupling domain code to a vendor SDK.
